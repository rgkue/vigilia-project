# Public URL for the teammate's FastAPI service. Leave empty for local demo mode.
VITE_API_BASE_URL=

# The browser sends synthetic cases to this endpoint when a backend URL is set.
VITE_INGRESO_PATH=/webhook/ingreso
